---
"caseparser": minor
---

Add Train-Case (`Content-Type`) and dot.case (`app.server.port`) conversions: `trainToX`/`xToTrain` and `dotToX`/`xToDot` for every existing case, with the same key type inference.
